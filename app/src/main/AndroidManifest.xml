<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:allowBackup="true"
        android:name=".NewtacksApp"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Newtacks">
        <activity
            android:name=".authentication.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.OnboardingActivity" />
        <activity android:name=".authentication.RoleSelectionActivity" />
        <activity
            android:name=".authentication.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".authentication.SignupActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".worker.account.WorkerReviewsActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".worker.WorkerProfileActivity" />
        <activity android:name=".worker.WorkerEditProfileActivity" />
        <activity android:name=".client.RefuseCompletionActivity" />
        <activity android:name=".ClientDashboardActivity" />
        <activity android:name=".WorkerDashboardActivity" />
        <activity android:name=".CompanyDashboardActivity" />

        <activity android:name=".CreateJobActivity" />
        <activity android:name=".company.CreateHiringActivity" />
        <activity android:name=".company.HiringDetailsActivity" />
        <activity android:name=".receipt.ReceiptDetailActivity" />
        <activity android:name=".chatbot.presentation.ui.TransactionChatActivity" />
        <activity
            android:name=".chatbot.presentation.ui.ChatActivity"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".utils.NotificationService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>